AuthorGram 12.9.2 verified Main and Play correction trigger.
Build exactly two signed release APKs: Main and Play. Do not generate or upload AAB artifacts.
In the Main-only iOS Message Menu, render a separate selected-message block above the action panel with the real sender or channel avatar, sender or channel name and the native ChatMessageCell content.
Keep Telegram's native reactions row above the selected-message block and never remove or merge it into the action panel.
Blur the complete chat background in iOS menu mode by using no exempt original message cell; keep only reactions, the independent selected-message preview and the action panel sharp above the blur.
Never apply preview-local blur and never leave the original selected cell elevated at its old chat position.
When the iOS composer contains any character, clear stale MENU drawable and Lottie state, hide and disable the media/menu container before animation early returns, and enforce the same invariant again after delayed animations.
Keep the actual chat overflow menu responsive while composer text is non-empty.
When composer text is cleared, restore the microphone or video-round container without changing classic or Play UI behavior.
Preserve every existing AuthorGram feature, package identity, signing identity, protected author_badge policy and launcher artwork.
Run Python syntax, source invariants, popup ordering, sender identity, blur ownership, composer state and badge checks before Android SDK setup.
Verify both APK packages, versions, signatures, zip alignment, integrity markers, Play publication policy and absence of raw protected badge identifiers.
Main and Play correction release request: 2026-08-06T11:26:00+02:00.
