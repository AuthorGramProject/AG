AuthorGram 12.9.2 audited economical Main ARM64 release.
This marker triggers exactly one PR-based release workflow from canonical dev.
The workflow must run the full zero-Gradle source/UI audit first and stop before expensive setup if any invariant fails.
Build exactly one signed arm64-v8a Main APK for fork.risin42.nagramx, versionName 12.9.2, versionCode 6991. Reject universal/non-arm64 APKs and all AAB output.
Synchronize and validate Play source for toss.authorgram.apk, but do not compile or upload a Play APK this month.
Trigger follows repair of the stale fixed-preview validator that caused run 31180671782 to fail before Gradle.
