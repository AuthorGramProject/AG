AuthorGram 12.9.2 single clean Main + Play ARM64 release.
This marker exists only to trigger the one observable PR-based release workflow.
The workflow must purge every superseded Actions run, disable obsolete workflows, verify the finalized AuthorGram source, build exactly one signed arm64-v8a Main APK and one signed arm64-v8a Play APK, reject universal/non-arm64 APKs and AAB output, verify package/version/signing identity and upload one final artifact package.
ARM64-only retry after failed run 31172839387; UI source remains canonical and unchanged by this trigger.
