name: AuthorGram Validation (maintenance lock)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maintenance-lock:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic validation builds are temporarily disabled during the approved AuthorGram update."
