# Final AuthorGram 12.9.1 release request

This non-application marker requests the single verified Main APK, Play APK and Play AAB build after the approved passphrase, encrypted-reply, branding, UI, package and workflow updates.
